<Window x:Class="Logement.TablePrint"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        xmlns:rdlcreport="clr-namespace:Microsoft.Reporting.WinForms;assembly=Microsoft.ReportViewer.WinForms" 
        mc:Ignorable="d"
        WindowStartupLocation="CenterScreen"
        Title="Impression" MinHeight="350" MinWidth="525"
        WindowState="Maximized">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <StackPanel>
            <TextBox Name="titre"  materialDesign:HintAssist.Hint="Titre"  />

            <Button  VerticalAlignment="Top" HorizontalAlignment="Center"  Click="refreshReport" Style="{StaticResource IconButton}">
                <materialDesign:PackIcon Kind="Refresh" />
            </Button>
        </StackPanel>

        <WindowsFormsHost Grid.Row="1" Name="ordonnance_viewer" >
            <rdlcreport:ReportViewer x:Name="_reportviewer" />
        </WindowsFormsHost>
    </Grid>
</Window>
